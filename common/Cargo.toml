[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4.12.1"

chrono = { version = "0.4.42", features = ["serde"] }

diesel = { version = "2.3.5", features = ["sqlite", "chrono", "returning_clauses_for_sqlite_3_35"] }

paste = "1.0.15"

serde = "1.0.228"
