[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }

diesel = { version = "2.3.5", features = ["sqlite", "chrono"] }

serde_json = "1.0.149"
serde = "1.0.228"

actix-web = "4.12.1"

paste = "1.0.15"
